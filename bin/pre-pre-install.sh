#!/usr/bin/env bash
# ###############################################################################
#   Dockerfile-level installations
#        This is (equivalent to and) at the level of Dockerfile.
#        If Dockerfile is used instead of this, the union layers provide a cahce meshanism for this.
#################################################################################

# install npm
# install python3
# install pip3
# install virtualenv
